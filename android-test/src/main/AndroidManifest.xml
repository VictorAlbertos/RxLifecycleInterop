<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.victoralbertos.rxlifecycleinterop">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">

    <activity android:name=".SampleActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity android:name=".SampleFragment">
    </activity>

    <activity android:name=".support.SampleAppCompatActivity">
    </activity>

    <activity android:name=".support.SampleAppCompatDialogFragment">
    </activity>

    <activity android:name=".support.SampleDialogFragment">
    </activity>

    <activity android:name=".support.SampleFragment">
    </activity>

    <activity android:name=".support.FragmentActivitySample">
    </activity>

    <activity android:name=".SampleDialogFragment">
    </activity>

    <activity android:name=".mvp.ActivityView">
    </activity>

    <activity android:name=".mvp.FragmentView">
    </activity>

  </application>

</manifest>